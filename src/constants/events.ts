const CONNECTION = 'connection';
const INIT_JOIN = 'initJoin';
const JOIN = 'join';
const SEND_MESSAGE = 'sendMessage';
const SEND_LOCATION = 'sendLocation';
const SEND_VOTE = 'sendVote';
const CLEAR_VOTE = 'clearVotes';
const SHOW_VOTES = 'showVotes';
const DISCONNECT = 'disconnect';

export default {
	CONNECTION,
	INIT_JOIN,
	JOIN,
	SEND_MESSAGE,
	SEND_LOCATION,
	SEND_VOTE,
	CLEAR_VOTE,
	SHOW_VOTES,
	DISCONNECT,
};
